// task4.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "iostream"
#include "bitset"

using namespace std;

int _tmain(int argc, _TCHAR* argv[])
{
	unsigned int A;
	unsigned int i;
	cin >> A;
	cout << bitset<32>(A).to_string() << endl;
	cin >> i;
	unsigned int res;
	res = A | (1 << i);
	//cout << bitset<32>(A).to_string() << endl;
	cout << bitset<32>(res).to_string() << endl;
	cout << res << endl;
	system("pause");
	return 0;
}

