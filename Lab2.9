// task9.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "iostream"
#include "bitset"

using namespace std;

int main()
{
	unsigned char n;
	int ntrans;
	cin >> ntrans;
	n = unsigned char(ntrans);
	unsigned char mask = 128;
	cout << ((n & mask) >> 7);
	n = n << 1;
	cout << ((n & mask) >> 7);
	n = n << 1;
	cout << ((n & mask) >> 7);
	n = n << 1;
	cout << ((n & mask) >> 7);
	n = n << 1;
	cout << ((n & mask) >> 7);
	n = n << 1;
	cout << ((n & mask) >> 7);
	n = n << 1;
	cout << ((n & mask) >> 7);
	n = n << 1;
	cout << ((n & mask) >> 7);
	system("pause");
	return 0;
}